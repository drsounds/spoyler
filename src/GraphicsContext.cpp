#include "GraphicsContext.h"
namespace spider {
    void GraphicsContext::setOrigo(const int& x, const int& y) {
    }
    GraphicsContext::GraphicsContext(WindowElement *window) {
        this->window = window;
    }
}
