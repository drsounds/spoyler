#ifndef IMAGE_H
#define IMAGE_H
#include <string>
#include <Observable.h>
#include <boost/thread.hpp>
#include <png.h>
using namespace std;
namespace spider {/*
class Image : public Observable
{
     protected:
        image *mImage;
        string *uri;
        boost::thread *worker;
    public:
        Image(string *uri);
        virtual ~Image();
        void download();
        void downloadImageAsync();

    private:
};*/
}

#endif // IMAGE_H
